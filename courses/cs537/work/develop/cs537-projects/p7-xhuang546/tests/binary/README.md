
## Binary Mapping

The public binary-driver tests build four anonymous programs named `bin1`
through `bin4`. The matching student-written runtime drivers are
`solution/runtime_1.c` through `solution/runtime_4.c`.

The source files in this directory intentionally use the same anonymous naming
scheme so that local testing mirrors the student-facing workflow.
